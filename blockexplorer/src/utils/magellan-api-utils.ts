export const baseEndpoint = '/v2';

export const transactionApi = baseEndpoint + '/transactions';

export const cTransactionApi = baseEndpoint + '/ctransactions';

export const xpTransactionApi = baseEndpoint + '/transactions';

export const cBlocksApi = baseEndpoint + '/cblocks';

export const cBlocksDetailsApi = baseEndpoint + '/ctxdata';

export const searchApi = baseEndpoint + '/search';

export const addressesApi = baseEndpoint + '/addresses';

export const assetsApi = baseEndpoint + '/assets';

export const transactionAggregatesApi = baseEndpoint + '/aggregates';

export const transactionFeeAggregatesApi = baseEndpoint + '/txfeeAggregates';
